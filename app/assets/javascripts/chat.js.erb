// Scroll the chat to the latest message on load

$(document).on('ready', function() {
  $('.messages.index').ready(function(){
    var $cont = $('.chat-window');
    if ($cont.length > 0) {
      $cont[0].scrollTop = $cont[0].scrollHeight;
      $('#message_body').focus();
    }
  });
});
